using UnityEngine;

public class SplatScript : MonoBehaviour
{
    [SerializeField]Texture2D splatTexture;

    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }

    public void Splat()
    {
        
    }
}
